# https://lefthook.dev/configuration/index.html
pre-commit:
  jobs:
    - run: cargo fmt
      stage_fixed: true
      glob:
        - "*.rs"
    - run: cargo clippy
      glob:
        - "*.rs"
    - run: cargo test
      glob:
        - "src/*"
        - "Cargo.toml"
